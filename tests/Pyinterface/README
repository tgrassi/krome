!************Tests the functionality of the Python interface************
! Uses the `iso_c_binding` standard Fortran module.
!
! After compiling, run `./kromeF`, `python test.py`, and then
! `python check_diffs.py` to check for differences in the outputs.
!
!***********************************************************************
